{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Watch API",
      "type": "process",
      "command": "dotnet",
      "args": ["watch", "run"],
      "problemMatcher": "$msCompile",
      "options": {
        "cwd": "${workspaceFolder}/src/Api"
      }
    },
  ]
}
